// import type { IMainStore } from '@/interfaces/index.interface'
// import { create } from 'zustand'

// const useMainStore = create<IMainStore>(set => ({
// 	mockery: 0,
// 	increaseMockery: (amount: number) =>
// 		set(state => ({
// 			mockery: state.mockery + amount,
// 		})),
// 	decreaseMockery: (amount: number) =>
// 		set(state => ({
// 			mockery: state.mockery - amount,
// 		})),
// 	message: '',
// 	setMessage: (newMessage: string) => set(() => ({ message: newMessage })),
// 	visitors: 0,
// 	setVisitors: (newVisitor: number) => set(() => ({ visitors: newVisitor })),
// }))

// export default useMainStore
